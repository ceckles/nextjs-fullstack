import type { NextConfig } from "next";

/**
 * Next.js configuration object.
 * 
 * Contains build and runtime configuration options for the Next.js application.
 * Currently using default configuration, but can be extended with custom settings
 * such as environment variables, redirects, rewrites, headers, etc.
 * 
 * @type {NextConfig}
 * 
 * @see {@link https://nextjs.org/docs/app/api-reference/next-config-js} Next.js Config Documentation
 */
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
